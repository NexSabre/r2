# 0.4.1
- Small improvement in logging. From now, the logs are stored at ~/.r2/logs/r2_[action_type]_[datetime_isoformat]

# 0.4
- Add an option to record/replay packages with custom name. record/replay option support --package arg

# 0.3.1
- Fix issue with creation a home directory at Windows 10

# 0.3
- Add error information to the response, when the content is not a json serializable

# 0.2
- Add a support for the url arguments. This improvement will change the format of the response's file. More information
you find at README

# 0.1
- Initial release (record and replay function should work - arguments on url does not work)
